import SwiftUI

struct CartTotalView: View {
    var body: some View {
        Text("Cart Total View")
    }
}

struct CartTotalView_Previews: PreviewProvider {
    static var previews: some View {
        CartTotalView().previewLayout(.sizeThatFits)
    }
}
